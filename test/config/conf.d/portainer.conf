   server {
        listen       80;
        server_name  192.168.50.28;

        #charset koi8-r;

        #access_log  logs/host.access.log  main;

        location / {

            root   html/dist;
            index  index.html index.htm;
        }
    }